//intial state
const state = {
  count: 3
};

//getters:
const getters = {
  count: state => {
    return state.count;
  }
};

//actions
const actions = {};

//mutations
const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations
};
