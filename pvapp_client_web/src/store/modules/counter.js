const state = {
    count: 4
};
const getters = {
    evenOrOdd: state => state.count
}

const actions = {};
const mutations = {};

export default {
    state,
    getters,
    actions,
    mutations
}